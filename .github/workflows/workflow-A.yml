name: 1st Main Workflow

on:
  push:
    branches: ["main"]

jobs:
  Printing_a_message:

    uses: faraz9993/gitnet2/.github/workflows/workflow-B.yml@v1
    environment: ${{ github.ref_name == 'main' && 'Approval' || null }}
    steps:
      - name: Printing a simple message 
        run:
          echo "This is Workflow-A"